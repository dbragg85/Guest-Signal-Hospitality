import NewsletterPageContent from '@/components/newsletter/NewsletterPageContent'

export const metadata = {
  title: 'Guest Signal Report | Newsletter Archive',
  description:
    'Guest Signal Report newsletter archive with issue-specific render modes and downloadable source files.',
}

export default function NewsletterPage() {
  return <NewsletterPageContent />
}
